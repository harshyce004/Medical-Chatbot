# MedBot

A simple React-based health chatbot that gives basic advice for common symptoms.

## Run locally

1. Clone this repo:
   ```bash
   git clone https://github.com/harshyce004/medbot.git
2. cd medbot
3.Install dependencies: npm install
4.Run app: npm run dev
